[pytest]
pythonpath = .
testpaths = tests
markers =
    glm52: GLM-5.2 cache-hit and billing correctness tests
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s %(levelname)s %(message)s
addopts = -s -ra --alluredir=reports/allure-results --clean-alluredir
